using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//this script manages all the shop and item interactions in the game
//data must be pre-loaded at the start of the scene other wise a lag spike will occure

public class PlayerPrefsManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
